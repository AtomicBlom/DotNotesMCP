namespace DotNotes.Server;

/// <summary>The console host. One binary, two modes: serving notes, and indexing them.</summary>
internal static class Program
{
	private static Task<int> Main(string[] args) => Task.FromResult(0);
}
